#pragma once
#include "Entity.h"

namespace EntityUtils
{
	/// <summary>
	/// Returns true if other entity is on left side of entitys
	/// </summary>
	bool IsOnLeftSideOf(LPEntity entity, LPEntity other);
};

